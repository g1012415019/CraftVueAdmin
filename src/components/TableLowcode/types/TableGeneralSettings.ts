export interface TableGeneralSettings {
  pagination?: boolean;
  pageSize?: number;
  showIndexColumn?: boolean;
  loading?: boolean;
  emptyText?: string;
  rowSelection?: boolean;
  rowKey?: string;
  scrollX?: number;
  scrollY?: number;
}
